export class Challenges {
    id?: string
    name?: string
    sponsor?: any
    description?: string
    reward?: string
    startDate?: Date
    endDate?: Date
    type?: string[]
    image?: string
    hashTag?: any[]
    for?: string[]
    mediaPreference?: string[]
    isDeleted?: boolean
    requiredImpressions ?: Number
    requiredParticipants ?: Number
    requiredPost ?: Number
}
